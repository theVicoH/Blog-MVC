CREATE TABLE IF NOT EXISTS Users
(
    id INT NOT NULL PRIMARY KEY AUTO_INCREMENT,
    username VARCHAR(255) NOT NULL,
    password VARCHAR(255) NOT NULL,
    email VARCHAR(255) NOT NULL,
    role VARCHAR(255) NOT NULL
);

CREATE TABLE IF NOT EXISTS Posts
(
    id INT NOT NULL PRIMARY KEY AUTO_INCREMENT,
    title VARCHAR(255) NOT NULL,
    content TEXT NOT NULL,
    user_id int NOT NULL,
    image TEXT NOT NULL,
    datetime DATETIME NOT NULL
);

CREATE TABLE IF NOT EXISTS Comments
(
    id INT NOT NULL PRIMARY KEY AUTO_INCREMENT,
    content TEXT NOT NULL,
    user_id int NOT NULL,
    post_id int NOT NULL,
    com_id int NOT NULL,
    datetime DATETIME NOT NULL
);

ALTER TABLE Posts ADD CONSTRAINT FOREIGN KEY (user_id) REFERENCES Users(id);

ALTER TABLE Comments ADD CONSTRAINT FOREIGN KEY (user_id) REFERENCES Users(id);

ALTER TABLE Comments ADD CONSTRAINT FOREIGN KEY (post_id) REFERENCES Posts(id);